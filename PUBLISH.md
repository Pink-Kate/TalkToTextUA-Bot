# Інструкція: Які файли публікувати в репозиторій

## ✅ ФАЙЛИ ДЛЯ ПУБЛІКАЦІЇ (додати в Git):

### Основні файли коду:
- ✅ `bot.py` - головний файл запуску бота
- ✅ `config.py` - конфігурація (без токенів)
- ✅ `handlers.py` - обробники команд та повідомлень
- ✅ `storage.py` - зберігання даних
- ✅ `transcription.py` - модуль транскрипції
- ✅ `utils.py` - утиліти

### Документація:
- ✅ `README.md` - головна документація
- ✅ `SETUP.md` - інструкція з налаштування
- ✅ `PUBLISH.md` - цей файл
- ✅ `BOTFATHER_DESCRIPTION.md` - опис для BotFather
- ✅ `IDENTITY.md` - брендинг та ідентичність

### Конфігурація:
- ✅ `requirements.txt` - залежності Python
- ✅ `env.example` - приклад файлу конфігурації (БЕЗ токену!)
- ✅ `.gitignore` - правила ігнорування файлів

## ❌ ФАЙЛИ НЕ ДЛЯ ПУБЛІКАЦІЇ (НЕ додавати в Git):

### Конфіденційні дані:
- ❌ `.env` - містить реальний токен бота
- ❌ `bot_old.py` - резервна копія з токеном в коді

### Системні файли:
- ❌ `__pycache__/` - кеш Python
- ❌ `*.pyc`, `*.pyo` - скомпільовані файли
- ❌ `venv/`, `env/` - віртуальне середовище
- ❌ `.vscode/`, `.idea/` - налаштування IDE

### Тимчасові файли:
- ❌ `*.log` - файли логів
- ❌ `*.tmp`, `*.temp` - тимчасові файли
- ❌ `*.ogg`, `*.mp3`, `*.wav`, `*.m4a` - завантажені аудіофайли
- ❌ `.whisper/` - кеш моделей Whisper

### Інші:
- ❌ `.DS_Store` (macOS)
- ❌ `Thumbs.db` (Windows)

## 📋 Команди Git:

### Спочатку перевірте статус:
```bash
git status
```

### Додайте файли для публікації:
```bash
# Додати всі файли, окрім тих, що в .gitignore
git add .

# Або додати конкретні файли:
git add bot.py config.py handlers.py storage.py transcription.py utils.py
git add README.md SETUP.md PUBLISH.md BOTFATHER_DESCRIPTION.md IDENTITY.md
git add requirements.txt env.example .gitignore
```

### Перевірте, що додано:
```bash
git status
```

### Зробіть commit:
```bash
git commit -m "Initial commit: модульний Telegram бот для розпізнавання мови"
```

### Відправте в репозиторій:
```bash
git push origin main
```

## ⚠️ ВАЖЛИВО:

1. **НЕ комітьте `.env` файл!** Він містить ваш токен бота.
2. **НЕ комітьте `bot_old.py`!** Він містить токен в коді.
3. **Перевірте `.gitignore`** - він має містити всі конфіденційні файли.
4. **Використовуйте `env.example`** як приклад для інших користувачів (БЕЗ реального токену).

## 🔍 Перевірка перед публікацією:

```bash
# Перевірте, що .env НЕ додається
git status | grep .env
# Має бути порожньо або показати, що файл ігнорується

# Перевірте, що bot_old.py НЕ додається
git status | grep bot_old
# Має бути порожньо
```

## 📝 Примітка:

Файл `.gitignore` вже налаштований правильно і автоматично ігнорує всі небезпечні файли. Просто використовуйте `git add .` - Git автоматично пропустить файли з `.gitignore`.


