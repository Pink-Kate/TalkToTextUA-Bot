# Налаштування Railway для TalkToTextUA-Bot

## 🔧 Додавання токену бота в Railway

Щоб бот працював на Railway, потрібно додати змінну середовища `BOT_TOKEN`.

### Метод 1: Через "+ New Variable"

1. Відкрийте ваш проєкт на Railway
2. Виберіть сервіс **TalkToTextUA-Bot**
3. Перейдіть на вкладку **Variables**
4. Натисніть кнопку **"+ New Variable"**
5. Введіть:
   - **Key:** `BOT_TOKEN`
   - **Value:** `<YOUR_BOT_TOKEN>`
6. Натисніть **"Add"** або **"Save"**

### Метод 2: Через Raw Editor

1. Відкрийте ваш проєкт на Railway
2. Виберіть сервіс **TalkToTextUA-Bot**
3. Перейдіть на вкладку **Variables**
4. Натисніть кнопку **"{} Raw Editor"**
5. Вставте наступний рядок (формат KEY=VALUE):
```
BOT_TOKEN=<YOUR_BOT_TOKEN>
```
6. Натисніть **"Save"** або **"Update"**

## ✅ Перевірка

Після додавання змінної:
- Railway автоматично перезапустить сервіс
- Статус зміниться з "Crashed" на "Running"
- Бот почне працювати

## 🔍 Перевірка логів

Якщо бот не запускається:
1. Перейдіть на вкладку **Logs** у Railway
2. Перевірте, чи немає помилок
3. Переконайтеся, що токен введено правильно (без пробілів, без лапок)

## 📝 Додаткові змінні (опціонально)

Ви також можете додати:
- `LOG_LEVEL=INFO` - для налаштування рівня логування (DEBUG, INFO, WARNING, ERROR)

## ⚠️ Важливо

- **НЕ діліться токеном** - він є секретним
- Токен має бути в форматі: `числа:букви_та_цифри`
- Після зміни змінних Railway автоматично перезапускає сервіс

